# AdsMaster API

FastAPI backend for AdsMaster - AI-powered Google & Meta Ads management platform.

## Quick Start

```bash
python3 -m poetry install
python3 -m poetry run uvicorn app.main:app --reload --port 8000
```

API docs: http://localhost:8000/docs
